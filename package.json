{
  "name": "nabocity",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pages:build": "npm run build",
    "pages:preview": "wrangler dev",
    "pages:deploy": "wrangler deploy"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "wrangler": "^4.111.0"
  }
}
